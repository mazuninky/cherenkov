# schema-validation

Phase 2 / M2 — first slice: per-namespace JSON Schema validation enforced
in `Hub::handle_publish`. AsyncAPI export and the TypeScript SDK
generator follow in subsequent changes.
